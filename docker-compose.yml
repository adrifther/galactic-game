version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: galacticgame-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Galactic
      POSTGRES_DB: GalacticGame
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

volumes:
  pgdata:
